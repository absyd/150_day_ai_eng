# Chain of Thought vs Direct Prompting Comparison
# Just with a prompt it does it wow ! :) 
## Results Overview

| Type | Question | Expected | Direct Response | CoT Response | Direct Tokens | CoT Tokens |
|------|----------|----------|-----------------|--------------|---------------|------------|
| Math | If a train travels 60 km in 1.5 hours, what is its average speed? | 40 km/h | 40 km/h | **Step‑by‑step reasoning**<br><br>1. **Recall the definition of average speed**<br>   \[<br>   \text{average speed} = \frac{\text{total distance traveled}}{\text{total time taken}}<br>   \]<br><br>2. **Insert the given numbers**<br>   - Total distance = 60 km<br>   - Total time = 1.5 hours<br><br>   \[<br>   \text{average speed} = \frac{60\ \text{km}}{1.5\ \text{h}}<br>   \]<br><br>3. **Perform the division**<br>   \[<br>   \frac{60}{1.5} = \frac{60}{\frac{3}{2}} = 60 \times \frac{2}{3} = 40<br>   \]<br><br>4. **Write the result with its units**<br>   \[<br>   \text{average speed} = 40\ \text{km/h}<br>   \]<br><br>---<br><br>**Final answer:** The train's average speed is **40 km per hour**. | 147 | 382 |
| Logic | All cats are mammals. Some mammals are black. Are all cats black? | No | No. | **Step‑by‑step reasoning**<br><br>1. **Interpret the premises in set‑theoretic terms**<br><br>   - Let **C** = the set of all cats.<br>   - Let **M** = the set of all mammals.<br>   - Let **B** = the set of all black things.<br><br>   *Premise 1*: "All cats are mammals."<br>   This means every element of **C** is also an element of **M** → \(C \subseteq M\).<br><br>   *Premise 2*: "Some mammals are black."<br>   This means there exists at least one element that belongs to both **M** and **B** → \(M \cap B \neq \varnothing\).<br><br>2. **What would "All cats are black" mean?**<br><br>   - "All cats are black" translates to \(C \subseteq B\).<br><br>3. **Check whether the premises force \(C \subseteq B\).**<br><br>   From the premises we only know:<br>   - All members of **C** are in **M**.<br>   - At least one member of **M** lies in **B**.<br><br>   Nothing in the premises tells us anything about the colour of the members of **C**.<br>   The "some" in the second premise could refer to a mammal that is *not* a cat (for example, a black dog, a black whale, etc.). Therefore the existence of a black mammal does **not** guarantee that every cat is black.<br><br>4. **Conclusion**<br><br>   The statement "All cats are black" does **not** logically follow from the given premises. It may be true in the real world, but it is not provable from the information provided.<br><br>---<br><br>**Answer:** No; the premises do not allow us to conclude that all cats are black. The argument is invalid. | 245 | 634 |
| Word Problem | John has twice as many apples as Mary. Together they have 18 apples. How many does Mary have? | 6 | 6 | **Step‑by‑step reasoning**<br><br>1. **Introduce a variable**<br>   Let<br>   \[<br>   M = \text{number of apples Mary has.}<br>   \]<br><br>2. **Express John's apples in terms of \(M\)**<br>   The problem states that John has twice as many apples as Mary, so<br>   \[<br>   J = 2M .<br>   \]<br><br>3. **Write the equation for the total number of apples**<br>   Together they have 18 apples:<br>   \[<br>   J + M = 18 .<br>   \]<br><br>4. **Substitute the expression for \(J\) into the total‑apple equation**<br>   \[<br>   2M + M = 18 .<br>   \]<br><br>5. **Combine like terms**<br>   \[<br>   3M = 18 .<br>   \]<br><br>6. **Solve for \(M\)**<br>   \[<br>   M = \frac{18}{3} = 6 .<br>   \]<br><br>7. **Interpret the result**<br>   Mary has 6 apples. (John then has \(2 \times 6 = 12\) apples, and indeed \(6+12 = 18\).)<br><br>---<br><br>\[<br>\boxed{6}<br>\] | 188 | 447 |
| Code | What is the bug?<br><br>for i in range(5):<br>    print(i)<br>    i += 1 | Modifying loop variable has no effect | The bug in the given code is that the variable `i` is being incremented after each print statement. This results in the loop iterating one less time than expected, as the value of `i` is incremented before the next iteration begins. To fix the bug, the line `i += 1` should be moved before the print statement.<br><br>Corrected code:<br><br>```python<br>for i in range(5):<br>    print(i)<br>```<br><br>Output:<br>```<br>0<br>1<br>2<br>3<br>4<br>``` | **Reasoning**<br><br>```python<br>for i in range(5):<br>    print(i)<br>    i += 1<br>```<br><br>1. `range(5)` creates an iterator that will yield the sequence **0, 1, 2, 3, 4**.<br>2. In a `for`‑loop the loop variable (`i`) is **assigned the next value from the iterator at the start of each iteration**.<br>3. The statement `i += 1` changes the local variable `i` *inside* the body of the loop, but **it does not change the iterator** that `for` is pulling values from.<br>4. When the next iteration begins, the `for` statement overwrites whatever value `i` had with the next value from `range(5)`.<br>   Consequently the `i += 1` has no effect on the sequence of values that the loop processes.<br>5. If the intention was to "skip" every other number (i.e., to get 0, 2, 4 …) the current code does **not** achieve that; it still prints 0, 1, 2, 3, 4.<br><br>**The bug**<br><br>The bug is the **unnecessary (and misleading) manual increment `i += 1` inside a `for` loop**.<br>In a Python `for` loop you should **not** modify the loop variable to control iteration; the iterator controls the values. If you need to step by a different amount, you should modify the `range` call (e.g., `range(0, 5, 2)`) or use a `while` loop.<br><br>---<br><br>**Correct ways to get the intended result**<br><br>*If you just want to print 0‑4 (no manual increment needed):*<br>```python<br>for i in range(5):<br>    print(i)<br>```<br><br>*If you intended to skip every other number (print 0, 2, 4):*<br>```python<br>for i in range(0, 5, 2):   # start, stop, step<br>    print(i)<br>```<br>or<br>```python<br>i = 0<br>while i < 5:<br>    print(i)<br>    i += 2<br>```<br><br>---<br><br>**Final answer**<br><br>The bug is that `i += 1` is trying to change the loop variable inside a `for` loop, but the `for` statement overwrites `i` on each iteration, so the increment has no effect. Use the `range` step argument (or a `while` loop) instead of manually incrementing `i`. | 176 | 568 |
| Algorithm | What is the time complexity of binary search? | O(log n) | O(log n) | **Step‑by‑step reasoning**<br><br>1. **What binary search does** –<br>   Given a sorted array of size \(n\), binary search repeatedly halves the search interval:<br>   - Compare the target value with the middle element of the current interval.<br>   - If they are equal, the search stops.<br>   - If the target is smaller, continue in the left half; otherwise continue in the right half.<br>   - Each step reduces the size of the interval from \(k\) elements to at most \(\lfloor k/2\rfloor\) elements.<br><br>2. **Number of steps until the interval becomes empty** –<br>   Starting with \(n\) elements, after one comparison we have at most \(n/2\) elements left, after two comparisons at most \(n/4\), and after \(t\) comparisons at most \(n/2^{t}\).<br><br>   The algorithm stops when the interval size drops to 1 (or 0). So we need the smallest \(t\) such that<br><br>   \[<br>   \frac{n}{2^{t}} \le 1 \quad\Longrightarrow\quad 2^{t} \ge n \quad\Longrightarrow\quad t \ge \log_{2} n .<br>   \]<br><br>3. **Complexity classification** –<br>   - The *worst‑case* (and also the *average‑case*) number of comparisons is proportional to \(\log_{2} n\).<br>   - The *best‑case* occurs when the middle element is the target on the first check, requiring only one comparison, i.e. \(O(1)\).<br>   - In asymptotic notation we focus on the dominant case, which is \(O(\log n)\).<br><br>4. **Space complexity (for completeness)** –<br>   The classic iterative binary search uses only a constant amount of extra variables, giving \(O(1)\) auxiliary space. (A recursive version uses \(O(\log n)\) call‑stack space.)<br><br>---<br><br>**Final answer**<br><br>The time complexity of binary search is **\(O(\log n)\)** (worst‑case and average‑case; best‑case is \(O(1)\)). | 152 | 438 |

## Performance Analysis

### Token Usage Comparison
- **Direct Prompting**: Average 161.6 tokens per response
- **Chain of Thought**: Average 473.8 tokens per response
- **CoT Overhead**: ~193% more tokens

### Response Quality
- **Direct**: Concise, accurate answers for simple problems
- **CoT**: Detailed reasoning, better for complex problems, educational value

### Use Cases
- **Direct**: Quick answers, simple calculations, straightforward logic
- **CoT**: Complex reasoning, educational contexts, error troubleshooting